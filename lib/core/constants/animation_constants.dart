abstract class AnimationConstants {
  // ************* Durations ************* //
  static const Duration generalDuration = Duration(milliseconds: 300);
  static const Duration pageViewDuration = Duration(milliseconds: 200);
  static const Duration selectorDuration = Duration(milliseconds: 300);
}
